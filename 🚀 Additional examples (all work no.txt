🚀 Additional examples (all work now)
Input:
Buy when price is above 150 and volume is above 500k


DSL:

ENTRY:
close > 150 AND volume > 500000

Input:
Buy when price closes above 10-day moving avg and volume > 2M


DSL:

ENTRY:
close > sma(close,10) AND volume > 2000000

Input:
Buy when price is above 200


DSL:

ENTRY:
close > 200

Input:
Buy if price crosses above the 50-day SMA


DSL (cross not added yet unless you ask to enable):

ENTRY:
close > sma(close,50